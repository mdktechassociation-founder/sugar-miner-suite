/// The web's twins for the two numbers [Host] needs from the operating system.
/// A browser exposes neither, and nothing on the web mines yet, so these are
/// stated constants rather than a guess dressed up as a measurement.
library;

int cpuCores() => 4;

int ramMb() => -1;
