package com.mdk.sugarminer

import io.flutter.embedding.android.FlutterActivity

/**
 * Nothing to customise: the mining lives in the background-service isolate and
 * the native yespower library, not in platform code.
 */
class MainActivity : FlutterActivity()
