name: sugar_miner_sdk_example
description: Shows how an app adds consented background SUGAR mining in a few lines.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sugar_miner_sdk:
    path: ../
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
