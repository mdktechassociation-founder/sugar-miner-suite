package com.mdk.sugarwallet

import io.flutter.embedding.android.FlutterActivity

/// Nothing special is needed here: the mining service, its notification and the
/// reboot receiver all come from the SDK's merged manifest. The app is the UI,
/// the wallet and the consent screen; the SDK is the engine.
class MainActivity : FlutterActivity()
